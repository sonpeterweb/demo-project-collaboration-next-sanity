# Sanity Studio env vars — only SANITY_STUDIO_* are exposed to the Studio bundle.
# Copy to sanity/.env and fill in your values.
SANITY_STUDIO_PROJECT_ID=your_project_id
SANITY_STUDIO_DATASET=production
